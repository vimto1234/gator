package main

const (
	commandExpectsNoArgs  = "%v command expects no arguments"
	commandExpectsOneArg  = "%v command expects one argument"
	commandExpectsTwoArgs = "%v command expects two arguments"
)
